<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="w-full min-h-screen bg-zinc-900 text-white flex items-center justify-center p-10">
    
    <div class="max-w-md w-full bg-zinc-800 p-8 rounded-2xl shadow-lg">
        <h3 class="text-3xl mb-6 text-center font-semibold text-gray-200">Reset Password</h3>

        <!-- Display Error Message if Exists -->
        <% if (errorMessage) { %>
            <div class="w-full px-4 py-3 mb-4 text-red-700 bg-red-100 border border-red-400 rounded-lg">
                <%= errorMessage %>
            </div>
        <% } %>

        <!-- Reset Password Form -->
        <% if (token) { %>
            <form action="/reset-password/<%= token %>" method="POST" class="space-y-4">
                <p class="text-gray-300 text-sm">Enter your new password below.</p>

                <input type="password" 
                    name="password" 
                    placeholder="New Password" 
                    required 
                    class="w-full p-3 rounded-lg bg-transparent border-2 border-zinc-700 focus:border-blue-500 outline-none text-white">

                <input type="password" 
                    name="confirmPassword" 
                    placeholder="Confirm Password" 
                    required 
                    class="w-full p-3 rounded-lg bg-transparent border-2 border-zinc-700 focus:border-blue-500 outline-none text-white">

                <button type="submit" 
                    class="w-full px-4 py-2 rounded-lg bg-blue-500 text-white font-semibold hover:bg-blue-600 transition">
                    Reset Password
                </button>
            </form>
        <% } else { %>
            <p class="text-center text-red-500 text-lg">The reset token is invalid or expired.</p>
        <% } %>
    </div>

</body>
</html>
