<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Account</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="w-full min-h-screen bg-[#1e1b29] text-white flex items-center justify-center p-6">
    
    <div class="max-w-md w-full bg-zinc-900 p-8 rounded-lg shadow-lg">
        <h3 class="text-3xl mb-6 text-center font-semibold">Login Account</h3>
        
        <!-- Login Form -->
        <form action="/login" method="POST" class="space-y-4">
            <input 
                class="w-full px-4 py-3 rounded-md outline-none bg-transparent border-2 border-zinc-700 text-white placeholder-gray-400 focus:border-blue-500 focus:ring-2 focus:ring-blue-500 transition"
                type="email" 
                name="email" 
                placeholder="Email" 
                required>
            
            <input 
                class="w-full px-4 py-3 rounded-md outline-none bg-transparent border-2 border-zinc-700 text-white placeholder-gray-400 focus:border-blue-500 focus:ring-2 focus:ring-blue-500 transition"
                type="password" 
                name="password" 
                placeholder="Password" 
                required>

            <!-- Remember Me -->
            <div class="flex items-center space-x-2">
                <input 
                    type="checkbox" 
                    id="remember" 
                    name="remember"
                    class="w-4 h-4 rounded-md border-2 border-zinc-700 bg-transparent focus:ring-2 focus:ring-blue-500 cursor-pointer appearance-none checked:bg-blue-500 checked:border-blue-500">
                <label for="remember" class="text-sm text-gray-300">Remember Me</label>
            </div>

            <button 
                class="w-full px-4 py-3 rounded-md bg-blue-500 text-white hover:bg-blue-600 transition font-semibold" 
                type="submit">
                Login
            </button>
        </form>

        <!-- Forgot Password -->
        <p class="text-sm text-center mt-4">
            <a href="/forgot-password" class="text-blue-400 hover:text-blue-300 transition underline">Forgot Password?</a>
        </p>
    </div>

</body>
</html>
