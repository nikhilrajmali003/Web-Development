<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Account</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white flex items-center justify-center p-10">
        <div class="max-w-md w-full">
            <h3 class="text-3xl mb-6 text-center">Login Account</h3>
            <!-- Login Form -->
            <form action="/login" method="POST" class="space-y-4">
                <input 
                    class="w-full px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-800 focus:border-blue-500" 
                    type="email" 
                    name="email" 
                    placeholder="Email" 
                    required>
                
                <input 
                    class="w-full px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-800 focus:border-blue-500" 
                    type="password" 
                    name="password" 
                    placeholder="Password" 
                    required>
                
                <div class="flex items-center space-x-2">
                    <input type="checkbox" id="remember" name="remember" class="rounded-md">
                    <label for="remember" class="text-sm">Remember Me</label>
                </div>

                <button 
                    class="w-full px-3 py-2 rounded-md bg-blue-500 text-white hover:bg-blue-600 cursor-pointer font-semibold" 
                    type="submit">
                    Login
                </button>
            </form>

            <!-- Forgot Password -->
            <p class="text-sm text-center mt-4">
                <a href="/forgot-password" class="text-blue-500 hover:underline">Forgot Password?</a>
            </p>

           </div>
        </div>
    </div>
</body>
</html>
