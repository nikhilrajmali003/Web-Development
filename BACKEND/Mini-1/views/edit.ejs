<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white">

    <div class="min-h-screen flex items-center justify-center p-6">
        <!-- Main Container -->
        <div class="max-w-2xl w-full bg-gray-800 rounded-lg shadow-md p-6">
            <!-- Header -->
            <div class="flex justify-between items-center mb-6">
                <h1 class="text-3xl">
                    <span class="font-light">Hello</span>, <span class="font-semibold"><%= user.name %></span>
                </h1>
                <a href="/logout" class="px-4 py-2 bg-red-500 text-white rounded-md hover:bg-red-600 transition">
                    Logout
                </a>
            </div>

            <!-- Edit Post Form -->
            <h5 class="text-xl mb-4">Edit your Post</h5>
            <form action="/edit/<%= post._id %>" method="POST" class="mb-8">
                <textarea 
                    name="content"
                    class="w-full resize-none p-3 bg-gray-900 border border-gray-700 rounded-md focus:border-blue-500 focus:outline-none text-white"
                    rows="4"
                    required><%= post.content %></textarea>
                <button type="submit" class="px-4 py-2 bg-yellow-500 text-white rounded-md hover:bg-blue-600 transition mt-3">
                    Update Post
                </button>
            </form>
        </div>
    </div>

</body>
</html>
