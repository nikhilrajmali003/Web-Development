<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="w-full min-h-screen bg-zinc-900 text-white flex items-center justify-center p-10">
    
    <div class="max-w-2xl w-full bg-zinc-800 rounded-2xl shadow-lg p-8">
        <!-- Header -->
        <div class="flex justify-between items-center mb-6">
            <h1 class="text-3xl">
                <span class="font-light">Hello</span>, 
                <span class="font-semibold"><%= user.name %></span>
            </h1>
            <a href="/logout" 
                class="px-4 py-2 rounded-lg bg-red-600 text-white hover:bg-red-700 transition">
                Logout
            </a>
        </div>

        <!-- Edit Post Form -->
        <h5 class="text-xl mb-4 font-semibold text-gray-300">Edit your Post</h5>
        <form action="/edit/<%= post._id %>" method="POST" class="space-y-4">
            <textarea 
                name="content"
                class="w-full resize-none p-3 bg-transparent border-2 border-zinc-700 rounded-xl focus:border-blue-500 focus:outline-none text-white"
                rows="5"
                required><%= post.content %></textarea>
            <button type="submit" 
                class="w-full px-4 py-2 rounded-xl bg-blue-500 text-white hover:bg-blue-600 transition font-semibold">
                Update Post
            </button>
        </form>
    </div>

</body>
</html>
